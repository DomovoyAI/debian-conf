
# Создадим МИНИМАЛЬНЫЙ preseed.cfg для тестирования встраивания в ISO

preseed_minimal = """#_preseed_V1
### ТЕСТОВЫЙ PRESEED - Минимальная конфигурация для проверки
### Debian 12 (Bookworm) - Автоматическая установка

#### Локализация
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

#### Сеть (DHCP - самый простой вариант)
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-test
d-i netcfg/get_domain string local
d-i netcfg/wireless_wep string

#### Зеркало репозитория
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

#### Учётная запись (ТЕСТОВЫЕ ДАННЫЕ - ПОМЕНЯЙТЕ!)
d-i passwd/root-login boolean false
d-i passwd/user-fullname string Test User
d-i passwd/username string testuser
d-i passwd/user-password password test123
d-i passwd/user-password-again password test123
d-i user-setup/allow-password-weak boolean true

#### Часовой пояс
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

#### Разметка диска (автоматически весь диск)
d-i partman-auto/disk string /dev/sda
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

#### Минимальный набор пакетов
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string openssh-server sudo
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

#### GRUB
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

#### Завершение (автоматическая перезагрузка)
d-i finish-install/reboot_in_progress note

#### Пост-установка (добавить пользователя в sudo)
d-i preseed/late_command string in-target usermod -aG sudo testuser
"""

# Сохраняем файл
with open('/home/user/preseed-minimal.cfg', 'w') as f:
    f.write(preseed_minimal)

print("✅ Создан файл: preseed-minimal.cfg")
print(f"📏 Размер: {len(preseed_minimal)} байт")
print("\n" + "="*60)
print("📋 СОДЕРЖИМОЕ:")
print("="*60)
print(preseed_minimal)
